import PageLayout from "../../components/layout/PageLayout";

type Props = {
	children?: React.ReactNode;
	// props....
};

//todo:-----PageMedia component-----://
const PageMedia = (props: Props) => {
	return <PageLayout>Halaman Media</PageLayout>;
};

export default PageMedia;
