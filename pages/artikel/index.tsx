import PageLayout from "../../components/layout/PageLayout";

type Props = {
	children?: React.ReactNode;
	// props....
};

//todo:-----PageArtikel component-----://
const PageArtikel = (props: Props) => {
	return <PageLayout>Halaman Artikel</PageLayout>;
};

export default PageArtikel;
